A basic Client - Server setup, written in Java

To run Server and Client successfully:

- Open Server bash file first, if you open Client bash file it will fail to connect
- Once Server file is open, you may open the Client bash file
- Simply enter the information as the required in the display window
- When disconnecting from the Client, ensure to do it through typing 'quit'